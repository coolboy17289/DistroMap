node_modules/
dist/
.vite/
# Python artifacts (the suggestion API lives at api/index.py now)
.venv/
__pycache__/
*.pyc
*.pyo
# Local-dev suggestion storage (file-mode fallback when KV isn't set);
# production uses Vercel KV, so we don't commit local submissions.
.cache/
# Misc
*.log
.DS_Store
.env
.env.*
